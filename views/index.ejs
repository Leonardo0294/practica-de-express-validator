<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Formulario de Registro</title>
  <!-- Incluir los estilos de Bootstrap -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>

<div class="container mt-5">
  <h1 class="mb-4">Formulario de Registro</h1>
  <% if (validaciones && validaciones.length > 0) { %>
    <div class="alert alert-danger">
      <ul>
        <% validaciones.forEach(validation => { %>
          <li><%= validation.msg %></li>
        <% }); %>
      </ul>
    </div>
  <% } %>

  <form action="/registrar" method="POST">
    <div class="mb-3">
      <label for="nya" class="form-label">Nombre y Apellido</label>
      <input type="text" class="form-control" id="nya" name="nya" value="<%= valores.nya || '' %>">
    </div>
    <div class="mb-3">
      <label for="email" class="form-label">Correo Electrónico</label>
      <input type="email" class="form-control" id="email" name="email" value="<%= valores.email || '' %>">
    </div>
    <div class="mb-3">
      <label for="edad" class="form-label">Edad</label>
      <input type="number" class="form-control" id="edad" name="edad" value="<%= valores.edad || '' %>">
    </div>
    <button type="submit" class="btn btn-primary">Registrar</button>
  </form>
</div>
<% if(typeof validaciones != 'undefinded') {%>
  <%validaciones.forEach(validacion)=>{%>
< class = 'alert alert-warning alert-dismissible fade show' role="alert">
<strong</strong></strong>

<!-- Incluir los scripts de Bootstrap -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>